DB_USERNAME=""
DB_PASSWORD=""
PG_PORT=5433
DATABASE_URL="postgresql://${DB_USERNAME}:${DB_PASSWORD}@localhost:${PG_PORT}/myapp?schema=public"
BE_PORT=7000
PUBLIC_WEB_URL=http://localhost:3000

# Subdomain configuration
# Base domain for subdomain routing (e.g., pasal.com for mystore.pasal.com)
BASE_DOMAIN=pasal.local